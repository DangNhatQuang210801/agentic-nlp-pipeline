# Vietnamese Data

The Vietnamese part uses the Universal Dependencies Vietnamese VTB treebank.

Clone it locally with:

```bash
git clone https://github.com/UniversalDependencies/UD_Vietnamese-VTB.git data/vietnamese/raw/UD_Vietnamese-VTB
```

Raw treebank files are ignored by Git.
