There is one directory for every project phase:
- Phase 0: `data`
- Phase 1: `baseline`
- Phase 2: `agent`
- Phase 3: should probably be notebooks
- Phase 4: should probably be notebooks

The public API should get exposed through `__init__.py`.
